# قواعد مشروع هدهد HUDHUDE (سابقًا هدهدي، ثم هدهد الصين، ثم تالين، وقبلها دلال) — تُقرأ قبل أي عمل

## الهوية (قرار صاحب المشروع ٢٤/٠٩/٢٦ بعد شراء النطاق hudhude.com)
- الاسم: **هدهد** بالعربية و**HUDHUDE** باللاتينية (كالنطاق)، وتحتهما **«بوابتك إلى الصين»**. قرار صاحب المشروع ٢٥/٠٩/٢٦: «اجعل اسم الموقع هدهد، فهو مقبول حتى باسم الدومين HUDHUDE» — كان «هدهدي» من ٢٤/٠٩. «هدهد» مذكّر: «هدهد يشتري… ويتولى»، «لماذا هدهد؟».
- الاستبدال ٢٥/٠٩ كان آليًا (`هدهدي` ⟵ `هدهد` في src وpublic وscripts وextension وdesign، والإضافة رُفعت إلى 1.7.2 لتغيّر اسمها). «الهدهد» اسم الطائر يبقى كما هو في التعليقات والشرح؛ الاستبدال الآلي حوّله مرة إلى «الهدهدي» — احذر. هذا الملف يُبقي «هدهدي» في سجلّ ما جرى يومها.
- أسماء الملفات الداخلية باقية (`hudhud-logo.svg`, `hudhud-extension.zip`) — لا يراها الزبون.
- **النطاق hudhude.com** مربوط من `wrangler.toml` (`[[routes]] custom_domain = true` للجذر وwww)، وwww يحوّل بـ301 إلى الجذر في `src/index.tsx`. **`workers_dev = true` يجب أن يبقى صريحًا**: أول نشر بعد إضافة النطاق أطفأ `dlal.tsallabi.workers.dev` دقيقتين ونصفًا (تحذير في سجل النشر لا خطأ) فسقطت فحوص ما بعد النشر — والإضافة وروابط ماي باي تستخدمه. اقرأ سجل النشر بعد أي تغيير في `wrangler.toml`، فالنجاح الأخضر لا يعني أن شيئًا لم يُطفأ.
- فحص النطاق من GitHub (بيئة المساعد محجوبة عن الخارج): `smoke.yml` و`live-tour.yml` يقبلان مدخل `url`.
- البريد الظاهر للزبونة `hello@hudhude.com` (كان `hello@dlal.ly` لنطاق لا نملكه). يحتاج توجيه بريد Cloudflare ليستقبل.
- الشعار: `public/hudhud-logo.svg` هدهد متحرّك (ينقر والعُرف مطويّ، ثم يفتح عُرفه مروحةً ويهزّ ذيله؛ يثبت مع «تقليل الحركة»)، بجانبه «هدهد **الصين**» وتحته HUDHUD. المصدر ومولّده: `design/hudhud/build.py`. أيقونة التبويب `public/favicon.svg` (الرأس والعُرف فقط) و`apple-touch-icon.png`.
- اللون: **القرفة**. `--brand:#B05A20` للأزرار والروابط (تباين ٤٫٨٥ مع الأبيض)؛ قرفة الشعار الفاتحة `#D9823F` لا تصلح لزرّ بنص أبيض (٢٫٧). الأحمر `#d3262b` وأخواته للأخطاء ويبقى.
- ملف الإضافة `hudhud-extension.zip`، والقديم `/talin-extension.zip` يحوّل إليه بـ301.

- **تصميم اللوحتين (الإدارة والشريك) بأسلوب بوابة أميال بألوان هدهد (٢٥/٠٩/٢٦)**: أرسل صاحب المشروع صور `portal.amyal.ly` وقال «انظر إلى جمال التصميم وتناسق الألوان، استخدم نفس ألوان هدهد وخطوطه». في آخر `public/style.css` تحت `.admin-body`: ترويسة عائمة مستديرة، قائمة أقراص والنشط متدرّج القرفة (`--brand-grad`)، خلفية دافئة، بطاقات 20 بكسل بظل ناعم، بطاقات أرقام بمربّع أيقونة (`.pd-ic`)، صفوف حالة بشريط رفيع، حلقة (`.pd-donut`) ومحافظ (`.pd-wallet`) وصف إجراءات (`.pd-actions`). **خط Cairo لم يكن يُحمَّل في اللوحتين أصلًا** (المتجر وحده) — صار في `src/views/dash.tsx`.

- **صفحة «كيف يعمل هدهد» بأسلوب أميال (٢٥/٠٩/٢٦)**: أرسل صاحب المشروع `https://www.amyal.ly`، فوضعتُ أقسامها في الرئيسية أولًا، فردّ: «اترك الصفحة الرئيسية كما هي تعرض البضائع… نحن موقع بيع منتجات لا تتبع شحنات». **الرئيسية بضاعة فقط** (ومعها قسم `.why` القديم الذي يحمل رقم الواتساب ويفحصه e2e). أقسام `.hm-*` كلها في **`/how`**: بطل `.how-hero`، شريط أرقام حقيقية، «أربع خطوات فقط»، «تتبّع طلبك» مع بطاقة مراحل (`TrackCard`)، مدن التوصيل (`CITIES`)، أسئلة شائعة، ودعوة أخيرة. `/pages/how` يحوّل إليها بـ301. **صفحة `/track`**: رقم الطلب + آخر 4 أرقام من هاتف التوصيل (لا يُكشف طلب برقم يُخمَّن)، مراحل من الدفع حتى التسليم مع رقم شحنة أميال. «📍 تتبّعي طلبك» و«🐦 كيف يعمل هدهد» في شريط الترويسة والتذييل. فحص e2e يرفض أي `.hm-*` على الرئيسية.

## التصميم الجديد على المتجر كله (قرار صاحب المشروع ٢٥/٠٩/٢٦)
- «لماذا لم نطبّق التصميم الجديد على كل الصفحات والأيقونات؟ … حتى الصفحة الرئيسية، حافظ على هيئتها وأنها تعرض البضائع، لكن أعطها الطابع الأنيق في الحواف والظلال والبطاقات». كنت جدّدت `/how` و`/track` وحدهما.
- **طبقة واحدة في آخر `public/style.css`** تحت `body:not(.admin-body)` (اللوحتان لهما طبقتهما): خلفية دافئة `#faf7f4`، كل صندوق (`card-box`، `summary`، `floor`، `form`، `pc-side`، `order-card`…) أبيض 18 بكسل بظل ناعم بلا حدّ رمادي، بطاقة المنتج بيضاء مستديرة ترتفع عند المرور، الأزرار أقراص متدرّجة القرفة، الحقول 12 بكسل بحلقة تركيز قرفية، البحث قرص، والتذييل كتلة داكنة دافئة بحواف 32 بكسل. **الهيكل لم يتغيّر** (نفس الـHTML والمحدِّدات)، فالفحوص كما هي.
- **صفحات المساعدة** (`Doc` في `src/views/pages.tsx`): رأس دافئ بمربّع أيقونة متدرّج، أقراص تنقّل بأيقوناتها (تمرّ أفقيًا في الجوال)، والمحتوى في بطاقة `.doc-body`؛ الخطوات بطاقات برقم متدرّج، والأسئلة بطاقات تُفتح بـ«+». الخصوصية والشروط صار لهما `active` لأيقونتيهما.
- **الأيقونات** `src/views/icons.tsx` (`<Ic n="truck" />`): خط واحد 1.8 بلون النص، بدل الإيموجي في الشريط العلوي والشريط السفلي وزر الدردشة ووسائل الدفع ومركز الحساب وخيار الشحن وتتبّع الطلب، وبدل رموز التذييل «f ◎ ✆ ♪». صنف الـsvg **`si`** لا `ic`: صفحة الخدمة تستعمل `.ic` للمربّع حول الأيقونة فكبرت الـsvg إلى 52 بكسل.
- **عطل وجدته في الطريق**: قاعدة `.pd{display:flex}` للوحة الشريك (24/09) بلا قيد، و`.pd` اسم صفحة المنتج أيضًا — فصارت صفحة المنتج في الحاسوب عمودًا واحدًا بصورة بعرض الشاشة يومًا كاملًا على الحي. صارت `.admin-body .pd`، وفحص e2e يتأكد أن صفحة المنتج عمودان في 1280 بكسل. **أي قاعدة للوحتين تُكتب تحت `.admin-body`.**
- **صفحات الأقسام والبحث** (`listPage`، طلب صاحب المشروع بعدها بساعات): في الحاسوب التصفية بطاقة بيضاء والخيار النشط قرص متدرّج، والعنوان شريط دافئ بعدد المنتجات (`.desk-title` يظهر فوق 900 بكسل **فقط** — `display:flex` بلا استعلام أظهره مكرّرًا في الجوال)؛ في الجوال الشرائح أقراص، وشريط الفرز الثابت بحافة سفلية مستديرة، ولوحة التصفية بحواف 26 بكسل وأقراص وزرّين متدرّجين، والقسم النشط بحلقة قرفية. **بحث بلا نتائج** صار بطاقة `.no-res` تسمّي الكلمة وتفتح «اطلب برابط» و«تسوّق الجديد» (فحص e2e ينقر الزر).
- **صفحة المنتج والسلة والدفع** (الطلب الثالث في اليوم نفسه): الصورة 24 بكسل بظل، والمصغّرات بإطار قرفي للمختارة، وأداة الكمية قرص بزرّين دائريين، و«الوصف/دليل المقاسات/الشحن والإرجاع» بطاقات تُفتح بـ«+»، وشريط «رأي الزبائن في المقاس» قرص دافئ؛ بنود السلة داخل بطاقة، والكوبون بطاقة بحدّ متقطع؛ طرق الدفع بمربّع أيقونة (`PM_IC` بدل إيموجي `PAYMENT_METHODS`)، والمختارة متدرّجة.
- **عطل حقيقي في صفحة الدفع وجدته أثناء التصميم**: `CouponBox` و`ShipPicker` نموذجان (`<form>`) **داخل** نموذج الطلب `#checkoutForm`. المتصفح يُسقط وسم النموذج المتداخل، فصار حقل الكوبون وزرّ «تطبيق» جزءًا من نموذج الطلب: **ضغط «تطبيق» في صفحة الدفع يرسل الطلب نفسه بلا خصم**. (خيار الشحن نجا مصادفةً بطريقة المتصفح في إغلاق النماذج.) الآن تأخذ الأداتان `fid` فتُكتب أدواتهما في مكانها بالسمة `form="cpf"`/`form="spf"`، والنموذجان الفارغان بعد `</form>` الطلب. فحص e2e يتأكد أن حقل الكوبون ينتمي لـ`/cart/coupon`، وأن «تطبيق» يعود للدفع برسالة بلا طلب، وأن تغيير الشحن يبقى في الدفع. **لا تضع `<form>` داخل `<form>` أبدًا** — استعمل السمة `form`.
- **حسابي والطلبات** (الطلب الرابع): عنوان الصفحة بشريطين قرفيين، بطاقات الطلبات برأس دافئ، الكوبونات تذاكر بقيمة متدرّجة، العناوين بطاقات (الافتراضي بحدّ علوي قرفي)، الإشعارات غير المقروءة بشريط قرفي، فقاعات الدردشة، ومراحل صفحة الطلب **خط زمني متصل** (`.track .st::after`). **في الجوال** الصفحات الداخلية (`.pc.sub`، كل ما عدا «نظرة عامة») تُظهر المحتوى قبل قائمة الحساب — كانت القائمة الطويلة تسبق التذكرة والطلبات.
- **«بحثوا ولم يجدوا» و«مشاكل واجهت الزوار»** في حركة الزوار (الثانية ٢٥/٠٩ مساءً بالعطل نفسه): `LIMIT` مرتّب بالعدد وحده فكان الترتيب بين المتساويات عشوائيًا (أسقط فحص e2e بعد تراكم عمليات البحث المحلية). صار `ORDER BY n DESC, last DESC` — الأحدث أولًا عند التساوي، وهو الأنفع لصاحب المشروع أيضًا.
- «0 طلب مُسلَّم» على الرئيسية (قسم «لماذا هدهد») يُستبدل بمدة الوصول حتى تبلغ الطلبات المسلَّمة 10، كما في `/how`.
- **الدخول والتسجيل** (الطلب الخامس): `AuthShell` في `src/routes/auth.tsx` — لوحة دافئة بالشعار وأربع مزايا بأيقونات، وبطاقة النموذج بجانبها (في الجوال النموذج أولًا). الحقول بأيقونة داخلها (`.fld`) وزر عين يُظهر كلمة المرور (`data-pw-eye` في `app.js`). **سطر الكوبون الترحيبي يُقرأ من القاعدة** (`WELCOME10` مفعّل وغير منتهٍ) ويختفي إن أُطفئ — لا وعد بخصم لا يعمل. المحدِّدات التي تستعملها الفحوص باقية (`input[name=phone|password]`، «دخول»، «أنشئ حسابًا»).
- **اللوحتان** (السادس والسابع): الإيموجي في القائمة الجانبية والترويسة وعناوين البطاقات (`h3`) وأزرار الإجراءات وبطاقات الأرقام صار `Ic` (أُضيفت أيقونات chart users trend steps ticket download bag folder refresh bot antenna plug coin ship key list ext logout factory hourglass alert camera globe search ban gear print eye). `L()` في `AdminShell` صار يأخذ اسم الأيقونة معاملًا. بطاقات «نظرة عامة» (`a.kpi.ic`) روابط إلى صفحتها بمربّع أيقونة **فوق** الرقم — بجانبه قصّ «102,238.9 د.ل» إلى «02,238.9». القائمة 236 بكسل فلا تلتف «التذاكر والإرجاع». ما بقي من إيموجي داخل نصوص الجداول والتنبيهات (⚠ في سطر) مقصود.
- **صفحة التتبّع** (الثامن): البطاقة التوضيحية بلا رقم طلب موسومة «مثال توضيحي» (`demo`) — كانت تعرض DL-2026-000123 «في الطريق» كأنه طلب حقيقي. الحقلان بعناوين وأيقونات في بطاقة، والطلب الحقيقي يعرض مدينة التوصيل ومنطقتها، وتحته ثلاث بطاقات: أين أجد رقم طلبي (طلباتي)، متى يصل (المدة من الإعدادات)، طلبك تأخّر (تواصل معنا).
- **فخّ في الفحص**: `page.check()` على راديو يرسل نموذجه بـ`onchange` **لا ينتظر التنقّل** (بخلاف `click` على زر إرسال)، فقرأ الفحص الصفحة القديمة مرة ونجح مرة. `Promise.all([page.waitForNavigation(), page.check(...)])`. وتعديل ملف في `src` أثناء تشغيل e2e يعيد تحميل `wrangler dev` فيُسقط فحصًا بلا ذنب — لا تعدّل الكود والتشغيلة جارية.

## المخاطبة عامة للجميع لا مؤنثة (قرار صاحب المشروع ٢٥/٠٩/٢٦)
- «الموقع لجميع الأجناس رجالًا ونساءً وأطفالًا — لا تقل قدّمي، اضغطي، تطّلعي؛ قل قدّم، اضغط، ادفع، استمر». كل نص ظاهر يخاطب بالصيغة العامة: «تسوّق، اطلب، تتبّع طلبك، أضف إلى السلة، تواصل معنا، راسلنا، سجّل الدخول، يصلك».
- والزبون **«الزبون/الزبائن»** لا «الزبونة/الزبونات» في اللوحتين أيضًا («زبون مسجّل»، «ادخل باسمه»، «وصل للزبون»). أسماء البضاعة («ملابس نسائية») وأسماء البذور لا تتغيّر.
- الحارس في `scripts/e2e.mjs` («مخاطبة عامة للجميع»): يمرّ على 22 صفحة للزائر والزبون ويرفض أي صيغة مؤنثة في `innerText` — أمسك «راجعي» في وصف منتج و«أكملي» في إشعار قديم لم يرهما فحص الكود، لأنهما **في القاعدة لا في الكود**. ترحيل 0038 أعاد كتابة الإشعارات المرسلة ووصف البذور. أي نص جديد يُكتب مؤنثًا يُسقط التشغيلة.
- ما زال مؤنثًا عمدًا: التعليقات في الكود وهذا الملف (سجلّ لا واجهة).

## القاعدة الأولى (من صاحب المشروع): نريد موقعًا حقيقيًا يعمل، لا صورًا ولا نماذج وهمية
- كل ميزة تُبنى تعمل من طرف إلى طرف على الشاشة: زر يُضغط فعلًا، صفحة تُفتح فعلًا، بيانات تُحفظ في القاعدة فعلًا.
- لا تؤكد إتمام أي شيء قبل تجربته بنفسك: شغّل `node scripts/e2e.mjs` (اختبار Playwright على الشاشة) وأضف فحوصات لكل ميزة جديدة، وراجع اللقطات بعينك.
- إن لم تستطع التحقق من شيء (مثل خدمة خارجية محجوبة) قل ذلك صراحةً ولا تدّعِ أنه يعمل.
- الإجابات محددة: أرقام، أسماء صفحات، ما نجح وما فشل وسببه. لا عموميات.
- لا تتوقف لتسأل "هل أكمل؟" ما دامت هناك مهام واضحة؛ سجّل القرارات ونفّذ.

## اللغة والمحتوى
- الواجهة عربية RTL بالكامل. أي بيانات تصل بالصينية من 1688 (عناوين، ألوان، مقاسات، موردون) تُترجم عند الاستيراد (`src/lib/translate.ts`: قاموس ثم ذاكرة ثم Workers AI). لا يُعرض نص صيني للزبونة.
- الزبونة لا ترى أبدًا رابط المصدر ولا سعره باليوان.

## التشغيل والاختبار
- إعداد جهاز جديد خطوة بخطوة: `docs/LOCAL-SETUP.md` (مُجرَّب على نسخة نظيفة) — الأوامر: `npm ci` ثم `npm run setup` ثم `npm run dev`. تثبيت Claude Code على ويندوز: `docs/CLAUDE-CODE-WINDOWS.md`.
- على ويندوز يكفي النقر مرتين على `start.cmd` في جذر المشروع (تحديث + تثبيت + قاعدة محلية + تشغيل).
- صاحب المشروع يعمل على **ويندوز**: لا تعطِه أوامر لينكس (`cp`, `export`, `&&` في PowerShell)؛ استخدم أوامر `npm run ...` التي تعمل على كل الأنظمة.
- محليًا: `npx wrangler d1 migrations apply dlal-db --local -c wrangler.local.toml` ثم `npx wrangler dev -c wrangler.local.toml` (النسخة المحلية بلا ربط Workers AI).
- **كل فحص يغيّر حالة مشتركة يجب أن يُعيدها — وإلا أفسد التشغيلة التالية.** تكرر هذا أربع مرات في ٢٢/٠٩/٢٦ وكلّف ساعة: فحص قاعد ٢٠ منتجًا ولم يُعدها فهبط المتجر من ١٣٣ نشطًا إلى ٣٣ وعاد البحث بلا نتائج؛ وفحص ترك وضع الدفع `live` مشيرًا إلى خادم وهمي مغلق فسقطت فحوص الدفع؛ وفحص ترك طلبًا مدفوعًا في طابور شاهين كل مرة حتى دفع طلب الفحص الأصلي خارج الصفحة الأولى. القواعد: (١) أعد ما غيّرته، وفي `finally` أو `uncaughtException` لا بعد النجاح فقط. (٢) اصنع عيّنتك بنفسك ولا تتّكل على بيانات تركها فحص آخر. (٣) لا تخترع مُحدِّدات جديدة ما دامت هناك خطوات تنجح في الملف — انسخها. (٤) أي فشل صامت (دخول، حفظ) أكّده بـ`expect` فورًا؛ كلمة مرور الزبونة تتغير في منتصف الملف.
- الاختبار الشامل: `npm run e2e` (يجب أن يبقى 100% ناجحًا قبل أي دفع)، و`npm run e2e:watch` يعرضه على الشاشة بمتصفح مرئي — استخدمه عند مراجعة ميزة أمام صاحب المشروع. اختبار الإضافة: `scripts/ext-test/README.md`.
- النشر: أي دفع إلى `main` ينشر تلقائيًا عبر GitHub Actions إلى Cloudflare Workers، ثم يشغّل فحص الموقع الحي (`smoke.yml`). لا تنشر يدويًا.
- جولة الموقع الحي بمتصفح فعلي: `npm run tour` (أو workflow `live-tour.yml` بعد كل نشر) تفتح الصفحات وتنقر الروابط والأقسام وصفحات المنتجات واختيار الشحن كما تفعل الزبونة، وتلتقط صورًا تُرفع كـ artifact. **قراءة فقط**: لا دخول ولا طلبات ولا استيراد. `smoke.yml` يفحص HTML بـ curl فلا يرى صورة مكسورة ولا تمريرًا أفقيًا في الجوال ولا خطأ جافاسكربت؛ الجولة تراها، وعلى البضاعة الحقيقية لا على البذرة.
- الأسرار في GitHub Secrets: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `IMPORT_TOKEN`، واختياريًا `MYPAY_*`.
- كلمات مرور البذور التجريبية لا تُستخدم على الموقع الحي.

## المال والشحن
- سعر المنتج يتبع طريقة الشحن المختارة (كوكي `ship`): البطاقة وصفحة المنتج والسلة والفرز والفلترة كلها تستخدم السعر المعروض نفسه، لا الجوي دائمًا.
- التوصيل داخل ليبيا لكل مدينة: `delivery_city_rates` سطر `المدينة = المبلغ`، والطلب يُحاسب بمدينة العنوان المختار لا بما كان معروضًا في النموذج.
- `src_month_limit`: سقف شهري لاستدعاءات المزوّد يوقف الاستيراد والإثراء التلقائيين عند بلوغه (الترجمة لا تتأثر).
- الكرون كل ساعة: مهام الزحف المستحقة + ترجمة ٤٠ عنوانًا + دفعتا إثراء (٥٠ منتجًا) بادئًا بالمحجوزات.
- لا نجوم مُختلقة: التقييم يظهر فقط إن وُجدت مراجعات حقيقية، وإلا «وصل حديثًا» أو عدد ما بيع عند المورد.

## الدفع والرصد (درس ٢٢/٠٩/٢٦: محاولة دفع حقيقية ضاعت بلا أثر)
- صاحب المشروع ضغط «ادفع» على طلب `DL-2026-000007` فوجد سلة فارغة: لا نجاح ولا فشل ولا رسالة. الدفعة بقيت `created` بلا `checkout_url`، و**لا سطر واحد في `payment_log`** — لأن التسجيل كان *بعد* الاتصال، فإن مات الطلب في منتصفه لم يبقَ ما يُقرأ.
- القاعدة: **كل اتصال خارجي يُسجَّل قبل إرساله** (`logStart` في `src/routes/pay.tsx` يكتب السطر ثم `logEnd` يُحدّثه بالرد). سطر بقي بحالة «أُرسل ولم يصل رد» = الطلب مات، وهذا بذاته معلومة.
- كل `fetch` إلى ماي باي له مهلة `AbortSignal.timeout(20s)`؛ بدونها يقتل Cloudflare الطلب بلا رسالة.
- `createPayment` مغلَّفة بـ`try/catch` في المسار أيضًا: أي انهيار يُسجَّل ويُعرض للزبونة كصفحة «تعذر بدء الدفع» تقول إن لا خصم وتذكر رقم الطلب والمرجع.
- محاولة سابقة عالقة على `created` تُغلق تلقائيًا عند بدء محاولة جديدة لنفس الطلب.
- `الوضع = حقيقي` لا يعني خصمًا: البيئة (`mypay_sandbox`) هي التي تقرر. المؤشر في اللوحة يعرض الاثنين معًا («حقيقي · ساندبوكس»).
- الجذر `mypay_base_url` يُصحَّح **عند الحفظ** لا عند العرض فقط؛ `api.mypay.ly` نطاق اخترعناه خطأً ولا وجود له.
- الإشعارات الـ401 المتكررة في السجل مصدرها `smoke.yml` نفسه (يرسل توقيعًا خاطئًا عمدًا بعد كل نشر) — تُخفى من سجل البوابة وتُعدّ أسفله حتى لا تُغرق المدفوعات الحقيقية.

## التسعير — ما ثبت عمليًا على الكتالوج الحي (٢٢/٠٩/٢٦)
- **الشحن الداخلي في الصين للطرد لا للقطعة**: يُقسَّم على `min_qty` في كل مسار (استيراد، تحرير، `reprice-all`, `/api/source/reprice`، لقطة التكلفة في `order_items`). قبل ذلك كان كيس بـ٠٫٠٥ يوان يُعرض بـ٨ د.ل و٨٠٠ للّوط.
- **وزن المورّد قد يكون غرامات في حقل الكيلو**: `normWeightG` في `src/lib/source.ts` يُعيد تفسير ما تجاوز ٥٠ كغ غراماتٍ ويهمل المستحيل. ثمانية منتجات حية سعّرت رفّ حمام بـ٥٤٬٦٠٠ د.ل لأن وزنه قُرئ ٦٥٠ كغ.
- **تساوي الجوي والبحري ليس عطلًا** في قطعة وزنها غرام: الشحن الدولي عليها أقل من نصف دينار في الطريقتين فيختفي الفرق عند التقريب (`roundPrice` يقرّب لأقرب نصف دينار تحت ٢٠).
- الأسعار **مخزّنة** في `price_lyd`/`price_sea_lyd`: أي تغيير في منطق التسعير أو الإعدادات يحتاج إعادة تسعير، وإلا بقي القديم على الرف. القناة بلا تسجيل دخول: workflow `source-check.yml` بمدخل `reprice=all`.
- بعد كل إعادة تسعير: راجع الطرفين (`ORDER BY price_lyd DESC` و`ASC`). قيمة شاذة هناك تعني بيانًا خامًا فاسدًا لا خطأ حساب.
- **المتجر يبيع بالقطعة — قرار صاحب المشروع في ٢٢/٠٩/٢٦**: «لا يمكن أن نجبر الزبون أن يشتري أكثر من قطعة إلا برغبته». `retail_max_moq=2` فكل منتج حدّه الأدنى قطعتان فأكثر يدخل **مخفيًا** ولا يصل الرف (ترحيل 0023 أخفى القائم). الأداة في `/admin/products` (فلتر `?moq=N` و`?pack=1` وزرّا إخفاء/إظهار دفعةً) تعيدها متى شاء — أُخفيت ولم تُمحَ.

## العناوين المكسورة والإنجليزية
- `mixedScript`: حرف عربي ملتصق بلاتيني بلا مسافة («زippers»، «الكitchen») — نص مكسور يراه الزبون. `goodArabic` ترفضه، ويدخل طابور الترجمة، و`sweepMashedTitles` تحذف الكلمة وحدها مجانًا بعد محاولتين. ١٣٧ منتجًا حيًا.
- الشرط التصاق حرفين بلا مسافة، فلا يمسّ «مقاس XL» ولا «USB» ولا «2XL».
- عنوان بلا حرف عربي واحد (٢٧ منتجًا) كان في الطابور نظريًا ولا يصله الدور أبدًا: الترتيب كان يدفنه تحت آلاف الصفوف. رُفع إلى المرتبة الثانية بعد الصيني.
- `tidyJoin` يحذف حرف العطف اليتيم من **طرفي** العنوان بعد أي حذف كلمة؛ كانت البداية فقط تُنظَّف.
- قيم المتغيّرات تُصاب بنفس العلة («الرetro الأسود»، «خaki»، «لون فiolet»). كانت `Translator.t` تُعيد أي نص بلا حرف صيني كما هو فلا يصل للترجمة أصلًا — صار المكسور يمرّ كما يمرّ الصيني.
- **الترجمة المكسورة**: عربية سليمة نحويًا لكنها ليست ترجمة العنوان. `brokenTitle(ar, src)` في `src/lib/translate.ts` يعيد سببها:
  1. **تكرار بلا مسافة واحدة** («الوسومالوسومالوسوم…»): حارس `degenerate` القديم يقسّم على المسافات فلم يرَ شيئًا. خمسة عناوين حية (شُعيرات، أقراط، دمبل، أحمرا شفاه). أُضيف `REPEAT_NOSPACE` و`GLUED`.
  2. **اسم المنتج ضائع** («حمراء مزيفة لديكور المنزل» من 嘉兰百合 زنبق الجلوريوزا): العنوان يبدأ بصفة.
  3. **كلمة لا أصل لها في الصيني** («مقابض للزلازل» من 登山杖 عصا تسلّق). الكلمة المشبوهة وحدها لا تكفي: خيمة إغاثة أصلها 抗震救灾 فيها «زلازل» صحيحة — **نشترط غياب أثرها في `title_src`**.
  السوابق العربية تلتصق بالكلمة («للزلازل» = لِ + الزلازل) فالتعبير النمطي يقبل `[وفبكل]{0,3}(ال)?` قبل الجذر وإلا لم يُمسك شيء.
  `BROKEN_SQL` تعريف واحد يخدم الطابور والإحصاءات وفلتر `/admin/products?broken=1`، والمكسور **يتصدّر** طابور الترجمة بعد الصيني.
- **ذاكرة الترجمة تُسمَّم**: جدول `translations` يحفظ الترجمة المكسورة، و`Translator.t` كان يُعيدها لأنها «عربية سليمة» — فبقي أربعة عناوين على حالها بعد **سبع** محاولات. الآن: سطر عنوان مكسور يُحذف من الذاكرة ويُسأل النموذج من جديد، وناتج مكسور لا يُحفظ أصلًا.
- **`tr.t` تُعيد النص الأصلي حين تعجز كل النماذج.** قبوله في `retranslatePending` يعني استبدال عنوان عربي مكسور بعنوان **صيني** — أسوأ. لا يُقبل إلا ناتج `goodTitle` غير مكسور، وإلا يُترك العنوان كما هو.
- **مسرد `GLOSS`** في `src/lib/translate.ts` يُمرَّر داخل الطلب للنموذج: `登山杖 = عصا تسلّق` (كان «زلازل»)، `风炮 = مفتاح صدمي هوائي` (كان «قنبلة»). أي كلمة تتكرر خطأً ⟵ أضفها هنا؛ أرخص من إعادة ترجمة لا تنجح أبدًا.
- **التفتيش الشامل** `POST /api/source/audit` (workflow `source-check` بمدخل `audit=yes` للتقرير أو `audit=fix` للوسم): يجري على الخادم حيث البيانات ويعيد أعدادًا وخمس عيّنات لكل نوع. قراءة فقط بلا كريدت. شغّله كلما دخلت بضاعة جديدة.
- **ما وجده على ١٣٬٧٩٤ عنوانًا في ٢٢/٠٩/٢٦** (بعد أن ظننت الكتالوج نظيفًا): ١٤٣ عنوانًا فيها **أبجدية ثالثة** («بال스타يل» كورية، «تنورةチュチュ» يابانية، «розية» سيريلية، «đế» فيتنامية) — `mixedScript` كان يفحص اللاتينية وحدها؛ و٢٧ **كلمة مكرّرة مرتين متتاليتين** («للسيارات للسيارات») والحارس يشترط ثلاثًا؛ و٢٦ عنوانًا **فوق ١٢٠ حرفًا** حشوَ كلمات؛ و٤٧ **عنوانًا يتقاسمه ثلاثة منتجات فأكثر** (أحدها على ١٧ منتجًا) فالزبونة ترى صفوفًا متطابقة الاسم.
- **الدرس**: «العدّادات صفر» تعني أن القواعد التي كتبتُها لا ترى شيئًا، لا أن الكتالوج نظيف. كل كاشف جديد يكشف طبقة كانت تحته.
- **D1 يرفض ما يزيد على ١٠٠ متغيّر مربوط في جملة واحدة** (`too many SQL variables`). دفعة تحديث بمعرّفات كثيرة تُكتب أرقامها حرفيًا بعد `Number.isInteger` لا بـ`?`. سقط التفتيش أول تشغيل حي على ٢٠٠ معرّف بينما مرّ محليًا على ٢٣. ثم سقطت **صفحة شاهين كلها بـ500** حين بلغت الطلبات المدفوعة ١٠١ (`IN (?,?,…)` لبنود الطلبات) — أي `ids.map(() => '?')` بلا سقف هو هذا العطل مؤجَّلًا. ولا تسمِّ عمودًا بكلمة محجوزة: `AS full` أسقط صفحة الزاحف (`near "full": syntax error`) — جرّب الصفحة بـcurl قبل تشغيل e2e الطويل.
- **D1 يرفض نمط LIKE/GLOB فوق 50 بايتًا** (`LIKE or GLOB pattern too complex` ⟵ 500)، والمحلي لا يرفضه فلا يراه e2e. الحرف العربي بايتان: نمط `'% ' || ترجمة` في `revertBadEnglish` أسقط **كل دفعة ترجمة على الحي ساعتين** (٢٤/٠٩/٢٦، ومعها كرون الترجمة) وبقي الطابور عالقًا على ١٬١٢٦ دون أن يلاحظ أحد. ومثله بحث المتجر: رابط ملصوق أو كلمة من ٢٥ حرفًا = صفحة خطأ للزبونة. الآن: كل نمط من نص متغيّر يمرّ بـ`likePat` في `src/lib/db.ts` (يقصّه إلى 50 بايتًا)، ومقارنة نهاية النص بـ`substr` لا بـLIKE، وفحص في e2e يمسح المصدر ويرفض `%${`. والترجمة في الكرون معزولة بـ`try` حتى لا يُسقط عطلها ما بعدها.
- **العنوان المكرّر** (٤٧ اسمًا على ١٦١ منتجًا): الفرق موجود في العنوان الصيني (خامة، نوع، ماركة، مقاس — أحد عشر مقصًا بالاسم نفسه أصلها `不锈钢` و`SK5` و`高枝剪` و`摘果剪` وماركتا 德力西 و佐川田) لكن النموذج طواه في وصف عام. الحل: تعليمة سياقية في نفس الطلب تقول إن الاسم مستعمل وتطلب التمييز.
  - التعليمة السياقية **لا تُقرأ من ذاكرة الترجمة ولا تُكتب فيها**: ناتجها يخصّ قطعة واحدة، ولو حُفظ لسرى على كل من يشاركها المصدر.
  - عنوان جديد يصطدم باسم منتج آخر يُرفض ويبقى القديم؛ التمييز الذي يصنع تكرارًا جديدًا ليس تمييزًا.
  - بعد **اثنتي عشرة محاولة** نكفّ عن القطعة: إعلانان متطابقان فعلًا عند المورّد لا يفرّقهما نموذج، والإصرار يسدّ الطابور. يظهران في تقرير التفتيش ليقرر صاحب المشروع.
- **`needs_tr`** عمود علامة يضعه التفتيش (ترحيل 0022): أي كاشف جديد يرفع العلامة بدل إعادة كتابة استعلام الطابور، والمعلَّم يتصدّر الدفعة، والعلامة **لا تُمسح إلا بترجمة سليمة فعلًا**.
- **فخّ ثانٍ في فحوص اللوحة**: `/admin/products` يعرض ٢٠٠ الأحدث فقط، وعيّنات التشغيلات المتراكمة تدفع العيّنة القديمة خارج الصفحة الأولى. ابحث عنها بـ`?q=<offerId>` لا بالاتكال على ظهورها.
- **العدّادات الخمسة في `/api/source/stats` يجب أن تبقى صفرًا**: `chineseVisible` و`mashedTitles` و`englishTitles` و`mashedVariants` و`brokenTitles`. أي ارتفاع = بضاعة جديدة دخلت بنص لا يُقرأ. (نُظِّفت كلها على الحي في ٢٢/٠٩/٢٦: ١٣٧ و٢٧ و٢٤ ⟵ صفر، بأربع دفعات ترجمة على Workers AI بلا أي كريدت مزوّد.)

## قيم الألوان والمقاسات الإنجليزية (٢٤/٠٩/٢٦)
- `Translator.t` كان يُعيد أي قيمة بلا حرف صيني كما هي، والعدّادات تبحث عن الصيني والمكسور فقط: فبقي على الرف الحي **٢٢٬٨٦٧ سطرًا** (٤٬٤٦٩ قيمة مميّزة) بالإنجليزية في منتقي اللون والمقاس — «Navy blue»، «Female XL»، «Wine red».
- وتحتها شظايا جدول المواصفات قُطّعت بالفواصل فصارت «ألوانًا»: «non-returnable]»، «Capacity»، «Main picture»، «Length (cm)». هذه **تُحذف** (`junkAttr`) لا تُترجم — وتُحذف **قبل** الترجمة عند الاستيراد، وإلا ترجمها النموذج إلى «غير قابل للإرجاع» فصارت لونًا عربيًا لا يلتقطه أي كاشف.
- `src/lib/attr-en.ts`: قاموس (`enAttr`) يغطي ٨٬٥٣٣ سطرًا بلا نموذج (ألوان، درجات، «رجالي/نسائي»، رمز طراز + لون «K06 أسود وأخضر»، «M (40-50 كغ)»، مقاس حذاء بقدمه)، و`needsEnTr` تترك رموز المقاسات والوحدات (XXL، cm، USB). الباقي (٣٬٧٩٥ قيمة، ١١٬٩١٥ سطرًا) للنموذج، و«Xxl» يُوحَّد «XXL» (`sizeCase`).
- `fixEnglishVariants` تعمل **بالقيمة المميّزة لا بالسطر** (القيمة الواحدة على مئات الأسطر) وتسجّل في `attr_seen` (ترحيل 0030) ما لا يحتاج شيئًا (tries=99) وما عجز عنه النموذج ثلاثًا — وإلا عادت القيم العصيّة إلى رأس كل دفعة. المحاولة تُعدّ فقط إن سُئل النموذج فعلًا: نفاد ميزانية الدفعة ليس عجزًا.
- **النقرة الميتة في المنتقي** (كشفها فحص هذه الميزة): لون لا يتوفر مع المقاس المختار تلقائيًا كان رماديًا والنقر عليه لا يفعل شيئًا بلا رسالة. الآن يُختار وينتقل المقاس إلى ما يتوفر معه (`public/app.js`)، كما في شي إن.
- **أول تشغيل حي أفسد (٢٤/٠٩/٢٦) — وأوقفته**: «Women's black» ⟵ «S»، «183cmx61cm» ⟵ «M» (إصلاح «رمز المقاس» في `Translator.t` بـ`/i` قرأ s في Women's وm في cm)، «1.38inch» ⟵ «38 سم»، و«2011 double short-coffee» ⟵ «قميص قصير» (النموذج أسقط رقم التصميم فتطابقت خيارات مختلفة). الحراسة الآن: `enOk` (حرف عربي + كل رقم في الأصل باقٍ) على ناتج النموذج **وعلى ذاكرة الترجمة**، ورقم التصميم يُفصل ويُترجم الباقي وحده، وإصلاح رمز المقاس لا يمسّ الإنجليزي. `revertBadEnglish` تُعيد ما كتبته ترجمة فاشلة إلى أصله ثم يُترجم من جديد. **إلغاء workflow لا يوقف طلبًا وصل الخادم** — الدفعة الجارية تكمل.
- **الجولة الثانية على الحي (بعد الحراسة)**: الأرقام بقيت، لكن الترجمة ضعيفة: «double short-coffee» ⟵ «قميص قصير» (اللون ضاع)، «Black A-line skirt» ⟵ «تنجيد أسود»، «lined» ⟵ «خطوط». السبب: القيمة الإنجليزية كانت تذهب إلى **أصغر نموذج بتعليمة كُتبت للصينية**. الآن `SYS_ATTR_EN` (تعليمة للإنجليزية الركيكة بأمثلة) على `TITLE_MODELS` (llama-4-scout أولًا)، و`colorsKept`: كل لون في الأصل يجب أن يبقى في الترجمة (سوداء = أسود، «أزرق بحري» مقبول لـnavy). ما ترجمه النموذج الصغير قبل ذلك يُعاد مرة واحدة (`created_at` قبل 13:15 في 24/09/26، نهاية نشر التعليمة الجديدة — حدّ أبعد جعل الدفعة تعيد ترجمة الجديد نفسه في حلقة).
- **القاموس يغلب النموذج على الرف**: إن غطّى القاموس لاحقًا قيمةً ترجمها النموذج بشكل مختلف («Bordeaux Red» ⟵ «بورجوازي أحمر») تُستبدل على الرف بقيمة القاموس تلقائيًا. **أي خطأ ترجمة يتكرر في خيار ⟵ أضفه إلى `COLORS`/`enAttr` في `src/lib/attr-en.ts`** فيُصلَح الحي في الدفعة التالية بلا نموذج. وناتج القاموس يُقبل بلا حرف عربي («40-41»، «XL») — حارس النموذج كان يرفضه فيبقى على رأس كل دفعة.
- **طقم البيجامة**: «double short» = 双短 **نصف كم وشورت**، «short long» = 短长 **نصف كم وبنطلون طويل**، «double long» = كم طويل وبنطلون طويل (`SETS` في `attr-en.ts`). المنتجات 477 و478 و495 أُصلحت بترحيل 0031 (بالقيمة كاملة) لأن ذاكرتها حُذفت قبل أن يصير إرجاع «رقم + ترجمة» ممكنًا.
- **جرّب ٢٠ قيمة حيّة على نموذج وهمي بالردود الحقيقية قبل أي تشغيل جماعي**: القاموس مُختبر، أما النموذج فيفاجئ.
- **النتيجة الحية مساء ٢٤/٠٩/٢٦ (18:14)**: الطابور صفر؛ بقي **٣١٦ سطرًا في ١٢١ منتجًا** من ١١٬٢٥٢ نشطًا (كانت ١٩٬٦٥٧ في ١٬٤٩٧) — كلها قيم يئس منها النموذج (`attr_seen` tries 3-98، ١٨٤ قيمة) من نوع «Three cats Gray» و«4215-black snoopy» و«070 melody-white». الطريق لها القاموس لا مزيد من النموذج: `ORDER BY` عدد أسطرها واكتب قاعدة للنمط المتكرر.
- **آخر ٣١٦ سطرًا (١٨٦ قيمة)** لا يجمعها نمط (أسماء رسومات «4215-black snoopy»، أكواد موردين «YBL-A219 Navy Blue (310 grams)»، «Beige SW-270 [Diameter 30cm») تُرجمت يدويًا في `src/lib/attr-manual.ts` (`MANUAL`، مطابقة كاملة بلا حالة حروف، تُقرأ أول `enAttr`). «القاموس يغلب النموذج» يطبّقها على الحي. قيمة جديدة تعلق ولا نمط لها ⟵ سطر هنا.
- العدّاد السادس في `/api/source/stats`: **`englishVariants`** — يجب أن يتّجه إلى صفر. القناة الحية: workflow `source-check.yml` بمدخل `translate` (يكمل ما دام `enLeft>0`).

## متجر تجزئة لا سوق جملة (٢٢/٠٩/٢٦)
- صاحب المشروع فتح «صندوق هدايا للهواتف والسماعات» فوجد أقل طلب ٢٠٠ قطعة. الإعلان لمصنع علب (`YIGAO PACKAGING PRINTING · 源头大厂 专属定制`) **يبيع العلبة الفارغة**، والسماعات في الصورة محتوى توضيحي. عنوانه «صندوق هدايا» صحيح، والمنتج هو الخطأ.
- القياس قبل أي قرار: من ١٤٬١٨٥ نشطًا **١١٬٩٣٧ أقلّ طلبها قطعة واحدة** و١٬٤٧٧ قطعتان و٤٤٩ ثلاث إلى خمس — أي **٩٧٫٨٪ تجزئة**. الجملة ٣٠٦ فقط (٢٫٢٪)، منها ٤١ عند ٢٠٠ فأكثر.
- `notRetail` في `src/lib/source.ts`: لوط جملة (`min_qty ≥ retail_max_moq`، الافتراضي ١٠ ويُضبط من `/admin/pricing`) **أو** إعلان مصنع تغليف/طباعة/OEM. الاستيراد يُدخله `status='hidden'` فلا يصل الرف أبدًا — وتسبق هذه القاعدة حَجْزَ المسودة، فالبضاعة التي لن تُباع لا تنتظر ترجمة.
- الكلمات قوية الدلالة فقط: `包装 印刷 纸盒 礼品盒 包装袋 包装盒 OEM 贴牌 代工`. **مستبعدة عمدًا**: `定制` و`厂家直销` و`源头厂家` — يرشّها الباعة كدعاية على بضاعة سليمة (حذاء ومجموعة مكياج وقعا في الشبكة عند تجربتها).
- الأداة في `/admin/products`: فلتر `?moq=N` وفلتر `?pack=1` وزرّا إخفاء/إظهار دفعةً. عكسية تمامًا ولا تمسّ إلا `active`/`hidden`.
- **«الآلات وحوامل العرض والقماش» كانت إنذارًا كاذبًا مني**: فحص العيّنة أظهر أنها بضاعة سليمة (معدّات رياضة، صناديق أدوات، ساعات، كابلات، رشاشات حديقة). الكلمات `设备 机械 道具 面料` يرشّها الباعة كدعاية أو تصف الخامة داخل عنوان فستان — **لا تُبنى عليها قرارات إخفاء**، تمامًا كـ`定制`.
- صاحب المشروع رفض إخفاءها وطلب **التوضيح**: `kindOf` في `src/lib/source.ts` يصنّف الإعلان (`rack` حامل عرض · `mannequin` مجسّم · `fake` صناعي · `prop` ديكور وتصوير · `sauna` بدلة ساونا)، ويُحفظ في عمود `products.kind` عند الاستيراد (ترحيل 0021 يملأ القائم). الرف يعرض شارة على البطاقة وسطرًا أزرق على صفحة المنتج يقول ماذا تستلم الزبونة بالضبط، والوصف التلقائي يكرّره.
- الأعداد الحية: ١٥٥ صناعيًا · ١٦ بدلة ساونا · ١٣ ديكور تصوير · ١٢ حامل عرض.
- **كاشف التوأم كان يقصّ الأرقام**: ضجيج السنة `20\d\d` حُذف من داخل رقم العرض (690202645884 ⟵ «690» و«45884») فصار عنوان من ثلاث كلمات أربعًا وحُكم عليه توأمًا — صار السنة رقمًا مستقلًا فقط `(?<!\d)20\d\d(?!\d)`. سقطت به تشغيلة لأن طابع الوقت صار يحوي «2026».
- **إعادة ما لمسه فحص الإثراء إلى حالته الأصلية** لا إلى «نشط»: الإثراء يبدأ بالمسودات، والإعادة الساذجة نشرت مسودتين بعنوان صيني.
- **فخّ في فحوص الاستيراد**: `sameProduct` يرفض أي عنوان أقل من أربع كلمات، وعنوان يشترك في ٦٠٪ من كلماته مع منتج قائم يُعدّ «توأمًا» فيُتجاهل الاستيراد **بصمت** (`skipped`). عيّنة الفحص تبقى في القاعدة بعد التشغيلة، فأبقِ عناوين العيّنات عند **ثلاث كلمات أو أقل** وإلا سقطت التشغيلة التالية لسبب لا علاقة له بها. (أسقط هذا تشغيلتين في ٢٢/٠٩/٢٦.)

## الترقيم (درس ٢٢/٠٩/٢٦)
- `listPage` في `src/routes/store.tsx` يخدم **كل** القوائم: `/c/:slug` و`/c/all` و`/search` و`/new` و`/sale` و`/trending`. عطل واحد فيه = كل الأقسام.
- دالتان لا واحدة: `link(k,v)` للفلاتر **تحذف `page` عمدًا** (تغيير الفلتر يعيد للصفحة الأولى وإلا وقعت الزبونة في صفحة ٩ فارغة)، و`pageLink(n)` للترقيم **لا تحذفه**. كانت أرقام الصفحات تستعمل `link` فتضع الرقم ثم تحذفه في السطر نفسه: كل نقرة على ٣ أو ٦ أو ٩ تعيد إلى الأولى، وتختفي ٩٠٪ من البضاعة عن الزبونة بلا أي خطأ ظاهر.
- **نفس العطل بوجه آخر في اللوحة**: `/admin/products` و`/admin/orders` كانتا تقفان عند `LIMIT 200` **بلا رقم صفحة واحد**؛ ما بعد المئتين موجود في القاعدة ولا يصل إليه أحد. صار لهما `Pager` بـ١٠٠ صفّ للصفحة. **أي قائمة فيها `LIMIT` بلا `OFFSET` هي بيانات مخفية عن صاحبها.**
- الفحص الحارس في `scripts/e2e.mjs`: ينقر «٣» فعلًا ويتأكد أن الرابط يحمل `page=3`، وأن المتصفح وصل، وأن **بضاعة الصفحة الثالثة غير بضاعة الأولى**، وأن «عرض المزيد» منها يذهب للرابعة، وأن تغيير الفرز يُصفّر الصفحة.

## الجوال على نمط شي إن (قرار صاحب المشروع ٢٣/٠٩/٢٦: «أريد هدهد طبق الأصل مثل شي إن في الجوال»)
- صفحة القسم/البحث (`listPage`) في ≤٩٠٠ بكسل: **البضاعة أولًا**. صف أقسام دائري (`.m-cats`)، ثم شريط ثابت: «موصى به ▾ · الأكثر مبيعًا · السعر ⇅ · تصفية» وتحته شرائح سريعة، ثم الشبكة. القائمة الجانبية الطويلة للحاسوب وحده — كانت تُعرض كلها فوق البضاعة.
- لوحة التصفية `#fsheet`: عمود مجموعات (القسم، اللون، المقاس، السعر، العروض) وبجانبه خياراتها، و«مسح» و«عرض النتائج». **نموذج GET عادي** يعمل بلا جافاسكربت؛ القسم يصل `cat=` فيحوّله الخادم إلى `/c/slug` مع باقي الفلاتر. الجافاسكربت يعطّل الحقول الفارغة فيبقى الرابط نظيفًا.
- فرز `sold` (الأكثر مبيعًا) وفلتر `deal=1` (عليها خصم = سعر مشطوب أعلى من المعروض بطريقة الشحن المختارة).
- المقاسات والألوان في الفلاتر **مرتّبة بعدد المنتجات** لا أبجديًا، والمقاس الذي يحمله منتج واحد يُسقط حين تكثر (`حجاب وشالات` كان يعرض ٣٠٠ مقاس معظمها «35cm*2*10»)، والمقاسات بترتيبها الطبيعي XS…5XL ثم الأرقام.
- درج ☰ (`/m/menu` جزء HTML يُجلب عند أول فتح، صور الأقسام من `catTiles` المخزّنة عشر دقائق)، و«الأقسام» في الشريط السفلي تفتحه.
- **فحص العرض في الجوال يُقارن بـ٣٩٠ لا بـ`innerWidth`**: في وضع الجوال تتّسع النافذة مع المحتوى فيمرّ الفيض. هكذا بقيت السلة ٤٦١ بكسل (تُسحب أفقيًا) وخرج ☰ من الشاشة دون أن يلاحظ فحص. `.grid>.card{min-width:0}` يمنع سطرًا لا يلتف من توسيع عمود الشبكة.

## الأرقام إنجليزية في كل الموقع (قرار صاحب المشروع ٢٤/٠٩/٢٦)
- «اجعل كل الأرقام في الموقع إنجليزية 1 2 3 لا ١ ٢ ٣» — بدأ من مدة أيام الشحن. **لا تكتب ٠-٩ في أي نص يُعرض**؛ اكتب 0-9.
- الأرقام تُنسَّق بـ`en-US` («1,234.5 د.ل»)، لا `ar-LY` («1.234,5» — النقطة فيها فاصل آلاف فتُقرأ «1.234» دينارًا واحدًا بأرقام إنجليزية). التواريخ تبقى `ar-LY` (أرقامها لاتينية أصلًا).
- حارس في `src/index.tsx` يحوّل أي ٠-٩ و٫ ٬ ٪ في كل ردّ HTML: النصوص الآتية من القاعدة (مدة الشحن في `settings`، عناوين ترجمها النموذج، ما يكتبه الأدمن بلوحة عربية) لا يمرّ بها الكود. و`latinDigits` في `src/lib/db.ts` يطبّع ما يُحفظ من `/admin/pricing` (وإلا `parseFloat('٣٫٥')` = NaN).
- الفحص في `scripts/e2e.mjs` يكتب مدة الشحن بأرقام عربية في اللوحة ثم يمرّ على ١٢ صفحة ويرفض أي رقم عربي على الشاشة.
- **فخّ في الفحوص**: `uniqTag` كان «ت» ملتصقة بحروف لاتينية («تku498») = عنوان «مكسور» عند `mixedScript`. تراكم ٢١١ منها محليًا فأزاح عيّنة فحص الكنس خارج دفعته. الوسم الآن أرقام. وصار `sweepMashedTitles` يمرّ صفحاتٍ بالمعرّف (`id < ?`) لا «أول ٢٠٠» بلا ترتيب — ما لا يُصلحه الحذف كان يسدّ الدفعة إلى الأبد.

## اكتشاف منتجات جديدة مجانًا (الإضافة 1.7.0 — ٢٤/٠٩/٢٦)
- سؤال صاحب المشروع: «ألا يمكننا أن نجلب بالإضافة مجانًا؟». البحث يطلب حسابًا، **صفحة المنتج لا**. الإضافة تمرّ على ~١٠٠ صفحة منتج في الساعة للإثراء، فتلتقط روابط `detail.1688.com/offer/{id}` الأخرى فيها (توصيات، منتجات المتجر) — بعد التمرير لأسفل الصفحة لأنها تُحمَّل عند التمرير.
- `POST /api/crawl/discover {from, ids}` يحفظ الجديد وحده في `discovered_offers` (ترحيل 0027) بقسم الصفحة التي وُجد فيها، ويعدّ **كل صفحة مقروءة ولو بلا روابط**. `/api/import/queue?v=1.7.0` يعيد `fresh` (حتى `discover_per_batch`، الافتراضي 20، يُضبط من بطاقة الاكتشاف في `/admin/crawler`؛ ٠ = إيقاف). الأقدم من 1.7.0 لا تستلمها وإلا بقي الشريط ناقصًا.
- الاستيراد بـ`page_url='ext:discover'` يمرّ بنفس القواعد (الجملة مخفية، التوأم الأغلى يُتجاهل، الصيني مسودة). صفحة لم تُقرأ مرتين ⟵ `failed`. سقف الطابور ٢٠٠٠٠.
- **لم يُتحقق بعد أن 1688 تعرض هذه الروابط لزائر غير مسجّل** — الحاوية محجوبة عن 1688. البطاقة تقول الجواب بعد أول دفعة: «قرأت N صفحة · وجدت M رابطًا». إن بقي M صفرًا بعد ٢٠ صفحة تظهر رسالة صريحة أن الطريق لا يعمل. راجع `SELECT SUM(links_new),SUM(pages) FROM crawl_runs` قبل أي ادعاء.
- **الحالة الحية في ٢٤/٠٩/٢٦ 15:00 (قبل التثبيت)**: المتصلة `crawler_version=1.6.0` لا 1.7.0، فـ`discovered_offers` فارغ و`pages=0` في كل دفعات `crawl_runs` — الاكتشاف **لم يعمل مرة واحدة** لأن صاحب المشروع لم يثبّت 1.7.0 بعد. الإثراء نفسه يعمل (١٠٠ منتج/ساعة، ~٩٩ مُثرى). لا تقل «الاكتشاف لا يعمل» قبل أن تتصل 1.7.0 وتقرأ ٢٠ صفحة.
- **ثبت أنه يعمل (٢٤/٠٩/٢٦ 19:24)**: بعد تثبيت 1.7.0 قرأت أول دفعة ١١ صفحة، **٩ منها فيها روابط**، و**٨١ منتجًا جديدًا** في ٦ أقسام — أي أن 1688 تعرض روابط التوصيات لزائر غير مسجّل (~٧ روابط جديدة لكل صفحة). الاكتشاف أسرع كثيرًا من الاستيراد: ١٠٠ صفحة/ساعة ≈ ٧٠٠ رابط، والاستيراد `discover_per_batch=20` في الدفعة. رفعه يطيل الدفعة (~١٥ ثانية لكل منتج).
- **1.7.1 (٢٤/٠٩/٢٦ 20:20)**: سؤال صاحب المشروع «لماذا لا تدخل مباشرة؟ وماذا لو وقع الجالب؟». لا يضيع شيء: كل رابط يُرسل للخادم بعد صفحته فورًا، وكل منتج يُحفظ وحده. لكن الجديد كان يُستورد **بعد** مئة منتج إثراء وفي **الدفعة التالية**، فإن أُغلق كروم (دفعتان من ست يومها) لم يدخل منه شيء. الآن: الجديد **أولًا** في الدفعة، ثم الإثراء، ثم `GET /api/crawl/fresh?taken=N` يعطي ما اكتُشف في الدفعة نفسها بما بقي من الحصة فيُستورد قبل نهايتها. `discover.mjs` يثبت الترتيب بسجل فتح الصفحات في خادم التجارب (`/tmp/fx-hits`).
- **العنوان الإنجليزي (٢٤/٠٩/٢٦ 21:20)**: أول منتجات الجلب المجاني دخلت الرف **نشطة بعناوين إنجليزية** («Front zipper sports bra…»، 15019-15022): الإضافة تقرأ `document.title` وهو إنجليزي لزائر غير مسجّل، والاستيراد كان يحجز الصيني وحده، و`Translator.t` يعيد أي عنوان بلا صيني كما هو — فلا يعرّبه حتى طابور الترجمة. الآن: `enTitle` في `src/lib/translate.ts` يُدخل العنوان الإنجليزي الترجمة (`SYS_TITLE` يقبل الصيني والإنجليزي)، والمنتج الجديد بلا حرف عربي يدخل **مسودة** ولا يُنشر إلا بعنوان عربي (`releaseHeldDrafts`، والإثراء لا ينشر مسودة بعنوان بلا عربي). الخيارات والأسعار كانت سليمة (القاموس عرّب الألوان، و11 يوان/100 غ = 31 د.ل).
- **الوزن المستحيل (٢٥/٠٩/٢٦، سؤال صاحب المشروع عن الجلب المجاني)**: ربطة عنق 40 كغ بـ3,380 د.ل، ونظارة 30 كغ بـ2,550، وسماعات 36 كغ — 150 منتجًا نشطًا. جدول مواصفات 1688 يكتب «40g» أو «Weight (g) | 40»، والإضافة كانت تقرأ `parseFloat` فتُسقط الوحدة ثم تعدّ ما دون 50 كيلوغرامات. العلاج: (١) الإضافة 1.7.3 تقرأ الوحدة من القيمة أو من صف العنوان (g، 克، kg، 千克، 公斤، 斤 = 500 غ) — `parse-test.mjs` فيه ست حالات. (٢) `plausibleWeightG` في `src/lib/source.ts` في كل استيراد: فوق `max(وزن القسم×10، 3 كغ)` في قسم خفيف (≤ 900 غ) أو بأقل من يوانين للكيلو = مستحيل ⟵ غرامات إن كان مضاعف 1000 ومعقولًا للقسم، وإلا بلا وزن (وزن القسم، ويعيد الإثراء قراءته). والوزن المحفوظ المستحيل يُصحَّح عند مرور الإثراء (كان `!ex.weight_g` يمنع أي تصحيح إلى الأبد). الخيمة الحقيقية 27.4 كغ بـ1400 يوان لا تُمسّ. (٣) ترحيل 0039 بالقاعدة نفسها يصحّح القائم ويمسح `price_sea_lyd`، و**الكرون صار يعيد تسعير كل صف بلا سعر بحري** (`repriceRows(db,'missing',400)` في `src/routes/api.ts`) — فأي ترحيل يغيّر الوزن يمسح السعر البحري ويترك الباقي للكرون.
- **ترحيل 0039 التقط 238 وفاته 38** (فرشاة دهان بـ0.26 يوان «20 كغ» = 1,685 د.ل في أدوات البناء): شرطه تجاوز عشرة أضعاف وزن القسم، وقسم 2 كغ سقفه 20 كغ. أُضيفت قاعدة لا تعرف القسم: **5 كغ فأكثر بأقل من نصف يوان للكيلو = مستحيل**، والقطعة الأرخص من 5 يوان تُعاد غرامات (ترحيل 0040). بعدها راجع `ORDER BY price_lyd DESC` على النشط: المتبقي فوق 1,000 د.ل خيام ومظلات ومعدّات حقيقية.
- **فوق 50 كغ مخفي** (قرار صاحب المشروع ٢٥/٠٩/٢٦ «أخفِ المظلات»): 5 مظلات مستودع 200–250 كغ بـ17,925–22,120 د.ل من الجلب المجاني — سعرها صحيح لوزنها لكنها ليست تجزئة تُشحن جوًّا. الاستيراد يُدخل ما فوق `MAX_WEIGHT_G` مخفيًا كالجملة، وترحيل 0041 أخفى القائم. وفي اليوم نفسه رفع `discover_per_batch` إلى 100 بطلبه (كتبتُه في `settings` 18:54:30 بجملة معالج اللوحة نفسها).
- **التسعير بلا وزن (٢٥/٠٩/٢٦، قرار صاحب المشروع)**: 68٪ من الجلب المجاني بلا وزن في الصفحة، و8,915 منتجًا نشطًا بلا وزن كانت تُسعَّر بوزن قسمها كاملًا — سكين معجون بـ1.5 يوان في أدوات البناء (2 كغ) بـ180 د.ل (نحو 2,100 منتج رخيص مثلها)، و«دمبل 5 كجم» بـ160 د.ل بوزن 1.4 كغ (خسارة شحن). الآن في `src/lib/source.ts`: `estWeightG(est, cny)` = الأقل من وزن القسم و150 غ لكل يوان (لا أقل من 100 غ)، و`titleWeightG` يقرأ «5 كجم/0.5KG/256g/2kg3kg» من العنوانين (الأكبر، ويمرّ بـ`plausibleWeightG`) ويرفض المدى والحمولة ووزن الجسم و«克» بلا رقم (夹克، 马克笔، 克米特) و斤، ولا يقرأ عناوين أقسام الملابس. **كل مسار تسعير** يستعملهما بدل `est_weight_g ?? 300` (الاستيراد، `repriceRows`، التحرير، إعادة التسعير الشاملة، لقطة التكلفة في الطلب). `repriceRows` يحفظ وزن العنوان. ترحيل 0042 مسح السعر البحري لكل منتج بلا وزن فيعيد تسعيره الكرون (400/ساعة) أو `source-check` بـ`reprice=800` مع `item_id=-` و`keyword=-` (وإلا استهلك استدعاءين مدفوعين).
- الاختبار الحقيقي: `xvfb-run -a node scripts/ext-test/discover.mjs` (دفعتان على صفحات وهمية فيها «看了又看»؛ يعيد المهمة والإعداد ويحذف عيّناته).

## المنافس: بزناس BizzNass (bizznass-co.com) — دراسة بمتصفح حقيقي في ٢٤/٠٩/٢٦
- الدراسة: workflow `competitor.yml` (مدخل `url` وروابط خارجية) يشغّل `scripts/competitor-tour.mjs` على خوادم GitHub — بيئة المساعد محجوبة عن الموقع وعن Google Play. قراءة فقط، والصور في الفرع `competitor-shots`.
- **ما هم**: وسيط شراء من **أمازون وعلي بابا** («أغلب المواقع العالمية»)، طرابلس (حي دمشق)، واتساب 218925505452 و+218947707472. الزبون يلصق رابط المنتج أو رابط الدفع ويرفع فاتورة، والأسعار **بالدولار**، ومحفظة بالدولار، وتتبّع الطلب، و«سداد فواتير».
- **قوتهم**: جمهور — ٤٠ ألف متابع على فيسبوك ونشر يومي (منشور بـ١٢٨ تفاعلًا)، وأكثر من ٥ آلاف تنزيل للتطبيق، وسنوات في السوق (فريق إعلانات منذ ٢٠١٧).
- **ضعفهم (مقيس)**: الموقع صفحة هبوط من قالب جاهز («Template Crafted by UIdeck»)، `lang=en dir=ltr` لموقع عربي، **٢٩٤ حرفًا فقط** من المحتوى، وأقسامه فارغة حتى تُمرَّر، وعرضه في الجوال **٤٣٥ بكسل من ٣٩٠** (العنوان مقصوص). التطبيق على Google Play لم يُحدَّث منذ **١٤/٠١/٢٠٢٤** («Fix connection issue»)، ويصرّح بأن **البيانات لا تُشفَّر**، ويُوزَّع أيضًا ملف APK مباشرة. تطبيق الويب يعرض بضاعة تجريبية قديمة (iPhone 14، MacBook Air 2020) بأسعار **«$0»** و«$» فارغة. لا كتالوج ولا أسعار شحن معلنة ولا تقييم (فيسبوك: «بلا تقييم حتى الآن — ٣ آراء»).
- **ما بنيناه ردًّا**: «اطلبي برابط» (`/request`) — بابهم الوحيد صار بابًا عندنا بجانب كتالوج جاهز بالدينار. أرقامنا يومها: ١١٬٤٠١ منتجًا نشطًا، و**طلب واحد وزبونة واحدة** — نحن الأقوى منتجًا وسعرًا ووضوحًا، وهم الأقوى جمهورًا. الفجوة تسويق لا برمجة.

## «اطلبي برابط» (٢٤/٠٩/٢٦)
- `/request`: الزبونة تلصق رابطًا أو نص المشاركة كله (`parseLink` يأخذ أول رابط ويعرف 1688 وتاوباو/تيمول وعلي بابا وأمازون وشي إن). جدول `link_requests` (ترحيل 0029): `new` · `ready` · `rejected`، وسقف ١٠ مفتوحة للزبونة.
- رابط 1688 لمنتج على الرف ⟵ صفحته فورًا (`?have=1`). رابط 1688 جديد ⟵ يدخل `discovered_offers` بـ`from_offer='request'` **ويتصدّر** طابور الإضافة، فتستورده مجانًا. `settleLinkRequests` تربط الطلب بمنتجه لحظة وصوله (بعد كل `/api/import`، ومن الكرون، ومن الصفحتين): نشط ⟵ جاهز وإشعار «منتجك صار في هدهدي»، مخفي ⟵ اعتذار بالسبب («يبيعه بالجملة فقط، أقل طلب N»)، مسودة ⟵ ينتظر ترجمته.
- غير 1688: `/admin/requests` — الفريق يضيف المنتج ثم «ربط وإشعار» (رقم المنتج أو رابطه `/p/…` أو رقم عرض 1688)، أو «اعتذار» بسبب تقرؤه الزبونة. `discover_per_batch=0` يوقف روابط 1688 أيضًا — الصفحة تنبّه بذلك.
- مداخلها: أول رابط في شريط الترويسة، ودرج ☰، و«طلباتي بالرابط» في حسابي. `justify-content:safe center` في الشريط: التوسيط العادي مع تمرير أفقي يقصّ أول عنصر فلا يُوصل إليه.

## روابط التواصل الحقيقية (٢٤/٠٩/٢٦)
- جولة المنافس على موقعنا نحن كشفت: التذييل `wa.me/218000000000` و`facebook.com` فارغ، والرئيسية وصفحة الطلب تعرضان واتساب البذرة **218910000000** — زبونة الدفع بالتحويل كانت تُرسل إيصالها إلى رقم لا يملكه أحد.
- الآن: تُضبط من `/admin/pricing` (بيانات التواصل). `realWa`/`realSocial` في `src/lib/db.ts` ترفض الوهمي، وأيقونات التذييل تمرّ بـ`/pages/go/:net` فتفتح الحساب الحقيقي أو صفحة خدمة الزبائن. بلا رقم حقيقي يُوجَّه إيصال التحويل إلى «راسلينا عن هذا الطلب».
- **ما زال على صاحب المشروع**: كتابة رقم الواتساب الحقيقي وصفحات التواصل في اللوحة.

## فيسبوك وإعلانات ميتا (٢٤/٠٩/٢٦)
- **الهوية على فيسبوك**: `design/social/build.mjs` يولّد صورة البروفايل 720×720 (تُعرض دائرة) والغلاف 1640×624 من الشعار نفسه، ومعاينة لهما في صفحة فيسبوك (حاسوب وجوال). الجوال يقصّ جانبي الغلاف فيبقى الوسط ~1110 بكسل، وصورة البروفايل تغطي **وسط أسفل** الغلاف في الجوال و**يمين أسفله** في الحاسوب العربي — لا شيء يُقرأ هناك. عدد المنتجات يُمرَّر `PRODUCTS=11,401` من القاعدة الحية لا يُخمَّن.
- **كتالوج المنتجات** `/feeds/meta.csv` لمدير التجارة (مصدر بيانات مجدول): النشط ذو الصورة وبلا صيني، السعر `49.00 LYD` (الجوي)، الرابط مطلق بوسم `utm_source=facebook&utm_medium=catalog`، الصور عبر `/img` برابط مطلق. **بلا سعر مشطوب**: `compare_price_lyd` ليس سعرًا سابقًا حقيقيًا وميتا ترفض التخفيض المختلق. مخزَّن ٦ ساعات.
- **بكسل ميتا ووسم إثبات النطاق**: يُضبطان من `/admin/pricing#meta` (`meta_pixel_id` أرقام فقط، و`meta_domain_verify` يقبل الوسم كاملًا ويحفظ رمزه). يُحقنان في حارس HTML في `src/index.tsx` لصفحات الزبونة وحدها (لا `/admin` ولا `/partner`)، والإعدادات مخزّنة دقيقة و`bustMetaCache()` عند الحفظ. الأحداث في `public/app.js` من سمات `data-px-*`: ViewContent وAddToCart (`#addForm`)، InitiateCheckout (`/checkout`)، Purchase (صفحة الطلب المدفوع، مرة واحدة لكل طلب عبر localStorage). معرّف المحتوى = `products.id` = عمود `id` في الكتالوج، وإلا لم تعمل الإعلانات الديناميكية.
- الفحص يقطع `connect.facebook.net` ويقرأ `fbq.queue` نفسه: لا يرسل الفحص شيئًا لميتا، ولا ينجح صدفةً إن حُمّل السكربت الحقيقي على جهاز صاحب المشروع.
- **ما لا أستطيعه من بيئتي**: الدخول إلى فيسبوك أو مدير الإعلانات أو النشر أو الدفع — لا موصّل ميتا، وfacebook.com محجوب. أعدّ كل شيء (الصور، النصوص، الكتالوج، البكسل، الخطة) وصاحب المشروع يضغط «نشر» و«ادفع».

## شركاء الشحن: ربط API ولوحة الشريك وأسعاره (٢٤/٠٩/٢٦)
- طلب صاحب المشروع: «الطلب يذهب إلى شركة الشحن مباشرة بعد تأكيد الدفع، ولوحة للشريك يغيّر فيها الحالة ويرفع الصور وينشئ فاتورة لكل مرحلة ويعدّل أسعاره، ولنا دينار على كل بند و٣٥٪ على البضاعة لا تظهر له».
- **الإرسال** (`src/lib/partner.ts`): `markOrderPaid` ⟵ `onOrderPaid` يحفظ لقطة المستحقات في `orders.partner_fees_json` (بأسعار الشريك يومها)، ثم إن فعّل الشريك الربط: سطر في `partner_dispatch` **قبل** الاتصال، POST JSON إلى `api_url` بتوقيع `X-Hudhude-Signature: sha256=HMAC(api_secret, الجسم)` ومهلة 20 ثانية. ما لم يرد بـ2xx يعيده الكرون (5×n² دقيقة، حتى 8). سطر بقي `sending` (مات الطلب) يلتقطه الكرون بعد دقيقتين.
- **ما يُرسل للشريك لا يحمل سعر البيع ولا ربحنا** (فحص e2e يرفض `total_lyd|markup|price_lyd` في الجسم): البنود برابط 1688 وسعر المورد باليوان والوزن، والزبونة، ومستحقاته هو.
- **واجهة الشريك** `/api/partner/v1` (`src/routes/partner-api.ts`) برمز `Authorization: Bearer api_token`: `GET /orders?status=`، `POST /orders/:code/status|photos|invoices`. الرمز والمفتاح يُولَّدان عند أول فتح لـ`/partner/api`، وزر «إرسال طلب تجريبي» يرسل `order.test`.
- **لوحة الشريك**: `/partner/order/:code` (الحالة بملاحظة، المستحقات، صور المراحل، فواتير المراحل مع صفحة طباعة `/partner/invoice/:id`)، `/partner/rates` (أسعاري)، `/partner/api`. الأدمن يفتح أي شريك بـ`?partner=ID` والنماذج تحمله.
- **الصور في R2** (فعّله صاحب المشروع ٢٤/٠٩/٢٦ بعد 403 «Please enable R2 through the Cloudflare Dashboard» — التفعيل لا يتم من API؛ الحاوية `hudhude-media` أُنشئت من الموصّل، الربط `MEDIA`): `order_media.r2_key`، وإن فشل الرفع تُحفظ في عمود `data` (BLOB) وينقلها الكرون (`moveMediaToR2`، 20 كل ساعة) أو زر النقل في `/admin/partners`. تُصغَّر في المتصفح إلى 1600 بكسل JPEG 0.82 (`form[data-compress]` في `public/app.js`)، والسقف 1.5 م.ب. `public=1` يُظهرها للزبونة في صفحة طلبها (`/orders/:code/photo/:id`)؛ الافتراضي لا، فقد تحمل فاتورة المورد باليوان.
- **الملكية**: `owns()` في `src/routes/partner.tsx` على كل `/order/:code/*` و`/item/:id/*` والصور والفواتير. فحص e2e بموظف شريك ثانٍ (`0920000077`) يتأكد من 403/404 — **يسأل الخادم بـ`page.request`** لا `page.goto`: المتصفح يعرض الصورة من ذاكرته المؤقتة بعد أن فتحها شاهين في النافذة نفسها (أعطى 200 كاذبًا).
- **التسعير بأسعار الشريك** (`computePrice` في `src/lib/pricing.ts`) يعمل فقط حين `pricing_partner_id>0`: البضاعة + `partner_goods_margin_pct` (35) + (عمولة% × البضاعة + 1) + (النقل الداخلي + 1)/أقل طلب + (كغ محاسبي × سعر الكيلو + 1) + الجمارك؛ والتوصيل = سعر الشريك + 1 (`partnerDelivery`). الرسم `partner_fee_margin_lyd`. أسعار الشريك تُنسخ إلى `pp_*` في الإعدادات (`syncPricingPartner`) لأن `computePrice` متزامنة.
- الاختيار من `/admin/partners`: «معاينة الأسعار» تحسب عشرة منتجات موزّعة على سلّم الأسعار (الآن مقابل بأسعار الشريك) **دون حفظ**، و«اعتمد» يرفض شريكًا بلا سعر شحن جوي ثم يعيد تسعير الرف كله. تعديل الشريك أسعاره وهو شريك التسعير يرفع `reprice_needed` فتظهر للأدمن لافتة «أعد تسعير الرف». مُتحقَّق يدويًا: المنتج 8 (25.8 يوان، حجم افتراضي 0.5 كغ) = 65 د.ل بالحساب اليدوي والآلي.
- **الهاتف بصيغة واحدة** (`normPhone` في `src/lib/auth.ts`): صفحة الموظفين حفظت موظف شاهين `218913509213` كما كُتب، والدخول يحوّله إلى `0913509213` فرُفض بكلمة مرور صحيحة. الآن يُوحَّد عند الحفظ، والدخول يقبل الصيغتين، وترحيل 0034 وحّد القائم.
- **«ادخل باسمه» والموظفون (٢٤/٠٩/٢٦)**: المالك وحده (`staff_role='owner'`) يفتح أي حساب (موظف، موظف شحن، زبونة) بزر في `/admin/staff` أو صفحة الزبونة (`POST /admin/as/:id`): جلسة ساعتين تحمل `imp_by` و`back_sid` (ترحيل 0035)، وشريط أحمر يحقنه حارس HTML في `src/index.tsx` على كل صفحة، و«عودة إلى حسابي» أو «خروج» يعيده إلى جلسته. لا انتحال داخل انتحال، ولا انتحال حساب مالك. كل مرة تُسجَّل في سجل النشاط. صفحة الموظفين: «تعديل» لكل موظف (الاسم، الهاتف، كلمة مرور اختيارية، الدور/الشركة — تغيير كلمة المرور أو الدور يُغلق جلساته)، و«حذف» يمحو الحساب أو يعطّله ويحرّر رقمه (`deleted-<id>`) إن كان له أثر في السجلات. الإضافة تبدأ بصلاحية «دعم الزبائن» لا «المالك».
- **صلاحية موظف الشركة**: دوره «موظف شريك شحن» وحده — لا شيء من «صلاحية الإدارة» (تلك لموظفي صاحب المشروع). لا يدخل `/admin` (403) ويرى طلبات شركته فقط. نموذج الإضافة يخفي خانة الصلاحية ويُلزم باختيار الشركة.
- **«لوحتي» للشريك (`/partner`، ٢٤/٠٩/٢٦)**: بطاقات (بانتظار الشراء + المتأخر أكثر من يومين، العالق في مرحلته بعد `STAGE_SLA`، قيد الشراء، مخزن الصين، في الطريق، وصل ليبيا، وصل للزبون، الشحنات)، أشرطة الطلبات حسب المرحلة (تفتح `/partner/all?status=`)، الشحنات في الطريق وموعد وصولها التقريبي (جوي 15 يومًا، بحري 40)، وآخر الحركات. **قائمة الشراء انتقلت إلى `/partner/queue`**. البطاقة تُطابق القائمة (فحص e2e).
- **الحساب بيننا**: مستحقات الشريك = مجموع `partner_fees_json.total` للطلبات الجارية والمسلَّمة، ناقص ما دفعناه؛ ولنا عليه = 70% من طلبات «العربون» المسلَّمة (حصّلها من الزبونة) ناقص ما سلّمه. الحركات النقدية في `partner_ledger` (ترحيل 0036) يسجّلها الأدمن في `/admin/partners#ledger`، و`partnerBalance` في `src/lib/partner.ts` تحسب الرقم نفسه للطرفين.
- **موظفو الشركة**: الشريك يضيفهم من `/partner/team` ⟵ `active=0, pending_approval=1` وإشعار للمالك ⟵ «قبول/رفض» في أعلى `/admin/staff` (`#pending`). دخولهم قبل القبول يقول «بانتظار موافقة إدارة هدهدي» (لا «كلمة مرور خاطئة»).
- **الافتراضي على الحي: `pricing_partner_id=0`** — لا يتغيّر أي سعر حتى يضع الشريك أسعاره ويعتمدها صاحب المشروع.
- ترويسة اللوحتين كانت مكسورة منذ إعادة تصميم ترويسة المتجر (حُذفت قواعد `.hdr-top/.hdr-links` فالتصقت الروابط)، واللوحتان في الجوال كانتا بعرض 571 من 390 لأن `.admin-body .dash` تغلب استعلام 800 بكسل. أُصلحتا في آخر `public/style.css`.

## التوصيل داخل ليبيا: أميال ومناطق المدن بالكيلومتر (٢٥/٠٩/٢٦)
- طلب صاحب المشروع: «قسم النقل داخل ليبيا في لوحة الشريك، والربط مع أميال، والانتقال من وصل المخازن إلى انتقل لأميال، وسعر النقل لكل مدينة وضواحيها حسب الكيلومترات، وينعكس عندنا في لوحة الأدمن».
- **أميال (ثبت من موقعها بمتصفح حقيقي عبر `competitor.yml` — بيئة المساعد محجوبة عن amyal.ly)**: بوابة تجّار `portal.amyal.ly/login` و`/register`، ورقم الشحنة بصيغة `AMY-284510`، و**لا واجهة برمجية منشورة**. فالطريق: الشريك يسجّل الشحنة في بوابتهم ويكتب رقمها عندنا. `courierCreate` في `src/lib/partner.ts` يرسل JSON (المستلمة، المدينة، المنطقة، مبلغ التحصيل) إن أعطت أميال رابطًا ومفتاحًا، ويقبل رقم التتبع من أي حقل شائع في ردّها — **لم يُجرَّب على أميال فعلًا**.
- **الحالة لا تتغيّر**: جدول `orders` مقيّد بـCHECK على الحالات، وإعادة بنائه على الحي أخطر من أعمدة (ترحيل 0037): `courier`، `courier_ref`، `courier_status` (with_courier · delivered · failed)، `courier_at`، `courier_note`. الطلب يبقى «جاهز للتسليم» وهو مع أميال، و«وصل للزبونة» يجعله `delivered` (بنقاطه). التسليم يُسجَّل في `order_events` ويصل الزبونة إشعار «في الطريق إليك» برقم الشحنة، وصفحة طلبها تعرضه مع رابط التتبع إن وُجد (`courier_track_url` بـ`{code}`).
- `/partner/delivery` («🚚 التوصيل داخل ليبيا»): جاهزة لم تُسلَّم (ومعها «تم التسليم» لمن يسلّم بنفسه)، مع أميال (وصل / تعذّر بسبب يعيده للجاهزة)، وإعدادات الربط. واجهة الشريك: `POST /api/partner/v1/orders/:code/courier`.
- **المناطق** `partner_zones` (مدينة، اسم المنطقة، من–إلى كم، السعر) يضعها الشريك في «أسعاري» (إضافة منطقة، أو قالب سريع: وسط 0–5، ضواحي 5–15، بعيدة 15–30، خارج المدينة 30–60). عند الدفع **حين يكون شريك التسعير مختارًا** وللمدينة مناطق: قائمة «المنطقة» تحت المدينة، والسعر = سعر المنطقة + رسم المنصة (`zoneQuote`)، وبلا اختيار فأقرب منطقة. الطلب يحفظ `ship_zone_id`/`ship_zone`، ومستحق الشريك عن التوصيل = سعر منطقته. مع `pricing_partner_id=0` لا يتغيّر شيء عند الزبونة.
- الأدمن: `/admin/partners#domestic` (لكل شريك: جاهزة، معها، تعذّر، سُلِّمت في 7 أيام، مناطقه، والطرود مع شركات التوصيل، وأسعار المناطق)، وسطر المنطقة ورقم الشحنة في صفحة الطلب.

## حركة الزوار (`/admin/analytics`، ٢٤/٠٩/٢٦)
- طلب صاحب المشروع: «حركة كل المستخدمين واهتماماتهم ومدنهم ودولهم والصفحات، وهل واجهتهم مشكلة، وكم أضاف للسلة وكم بدأ الشراء ولم يُتمّ — لنتابعه بإعلانات وتذكير».
- جدول `visits` (ترحيل 0036): `vid` (كوكي زائر سنة)، `user_id`، `kind` (view · product · category · search · cart_view · cart · checkout · purchase · error)، `ref`، `n` (نتائج البحث)، الدولة والمدينة من `request.cf`، والجهاز. يُحذف ما فوق 90 يومًا في الكرون.
- التسجيل في حارس HTML في `src/index.tsx` بعد الرد (`waitUntil`)، والإضافة للسلة في `/cart/add`، وأخطاء جافاسكربت من `public/app.js` إلى `POST /t/e`. **لا يُحسب**: الروبوتات وHeadlessChrome (جولة الموقع الحي وفحص ما بعد النشر)، والموظفون والشركاء. e2e يضع كوكي `hh_track=1` ليُحسب عمدًا.
- **فخّ**: كوكي `vid` يُضبط في وسيط المستخدم **قبل** بناء الصفحة — ضبطه بعد قراءة جسم الرد في الحارس أسقط الصفحات بـ500 («This ReadableStream is disturbed»). والمسار يُحفظ مفكوك الترميز (`/p/فستان` لا `%D9%81…`).
- الصفحة: مسار الشراء (زوار ⟵ شاهدوا منتجًا ⟵ أضافوا للسلة ⟵ صفحة الدفع ⟵ طلبوا ⟵ دفعوا)، الدول والمدن (أسماء عربية لمدن ليبيا في `cityAr`)، الأقسام والمنتجات الأكثر مشاهدة، البحث و«بحثوا ولم يجدوا» (بضاعة مطلوبة لا نملكها)، المشاكل (404 وأخطاء الخادم وجافاسكربت)، **السلال المتروكة** و**بدأت الدفع ولم تُكمله** مع «إشعار» داخل الموقع و«واتساب» برسالة جاهزة، وآخر الزوار ومسار كل زائر (`/admin/analytics/v/:vid`). لإعلانات ميتا: البكسل يسجّل AddToCart/Purchase فيُبنى جمهور «أضافوا ولم يشتروا» من مدير الإعلانات.
- على الحي: الدولة والمدينة من Cloudflare فعلًا؛ محليًا يظهر موقع الوسيط (Austin) لا الزائر.
- **ماسحات الثغرات (أول ليلة على الحي، 25/09/26 00:53)**: 38 سطرًا، **17 منها «مشاكل» هي طلبات `/.env` و`/.git/config` و`/.aws/credentials` و`*.php`** من مراكز بيانات (باريس، فرانكفورت، أمستردام)، كل سطر بزائر جديد (بلا كوكي). `PROBE` في `src/lib/track.ts` يُسقطها من الحساب (المسار يبدأ بنقطة، أو امتداد خادم/نسخ احتياطي، أو `wp-`/`phpmyadmin`/`setup`…)، وفحص e2e يفتحها بزائر محسوب ويتأكد أنها لا تظهر. زيارات مراكز البيانات الأخرى بلا كوكي ما زالت تُعدّ زوارًا — راقب نسبة `COUNT(DISTINCT vid)=COUNT(*)` قبل قراءة الأرقام كزبائن.

## البنية
- Cloudflare Workers + Hono JSX + D1 (SQLite) + Workers AI. الملفات: `src/routes/*` الصفحات، `src/lib/*` المنطق، `migrations/*` المخطط، `extension/` إضافة كروم، `scripts/` الاختبارات.
- الأدوار: `src/lib/perm.ts`. الطلبات: `src/lib/orders.ts`. الدفع: `src/lib/mypay.ts`. مصادر 1688: زر الاستيراد، إضافة المتصفح (`/admin/crawler`)، مزوّد API (`/admin/source`).
- أي تغيير في المخطط = ملف ترحيل جديد مرقّم؛ لا تعدّل ترحيلًا قديمًا.

## صفحات 1688 بلا تسجيل دخول — ما ثبت عمليًا (تشخيص من متصفح صاحب المشروع)
- صفحة المنتج `detail.1688.com/offer/{id}.html` تفتح كاملة لزائر غير مسجّل: العنوان (بالإنجليزية في `document.title` مع لاحقة ` - 阿里巴巴`)، السعر في عنصر صنفه يحوي price، الصور من `cbu*.alicdn.com/img/ibank/` (أيقونات الواجهة من `img.alicdn.com/imgextra` وتُستبعد)، وجدول مواصفات أعمدته أزواج يحوي **الألوان والمقاسات والوزن**.
- **لا يوجد `__INIT_DATA__` إطلاقًا** في الصفحة بلا تسجيل دخول؛ القارئ يعتمد على DOM. لا تبنِ عليه.
- المحجوب خلف الدخول: جدول SKU التفصيلي فقط (سعر ومخزون كل مقاس). نبني المتغيرات من جدول المواصفات بدله.
- صفحة البحث `s.1688.com` تتطلب تسجيل دخول وتحوّل إلى `login.taobao.com`. البحث عبر OTAPI أو حساب الشريك في الصين.
- **صاحب المشروع لا يملك حساب 1688 ولن يملكه** (التسجيل للتجار الصينيين). هذا ليس عائقًا: مهمة الإضافة من نوع `stock` تفتح **صفحة المنتج وحدها** وهي تعمل لزائر غير مسجّل. صفحة البحث `s.1688.com` هي الوحيدة التي تطلب حسابًا، وكل مهام البحث `runner='server'` تمر بالمزوّد. **لا تطلب منه تسجيل دخول** — طلبتُه مرة في ٢٣/٠٩/٢٦ فعطّلت المهمة كلها بلا سبب.
- **مهام البحث للخادم وحده** (٢٤/٠٩/٢٦): حوّل صاحب المشروع ثماني مهام بحث إلى «الإضافة (مجانًا)» لأن القائمة سمحت، فعادت كلها «ok — جديد 0»: 1688 يحوّل صفحة البحث إلى `login.taobao.com` والقارئ لم يكن يعرف هذا النطاق. الآن: القائمة لا تعرض «الإضافة» لغير `stock`، والخادم يرفضها برسالة، و`/api/crawl/jobs` لا يعطي الإضافة إلا `type='stock'`، وترحيل 0026 أعاد الثماني للخادم، و`blocked()` يعرف `login.taobao.com`.
- `blocked()` في `extension/content.js` يفرّق بين `captcha` و`login`: الأولى يحلّها في دقيقة ويعيد التشغيل، والثانية تعني أن صفحة المنتج لم تعد تفتح لزائر غير مسجّل — لا جدوى من إعادة المحاولة، وهي تغيّر في 1688 يحتاج خطة أخرى. الإشعار يقول أيًّا منهما.
- **ارفع رقم نسخة الإضافة مع كل تغيير في `extension/`** (`manifest.json` و`EXT_VERSION` في `src/routes/admin-ops.tsx` معًا، وفحص e2e يحرس تطابقهما). في ٢٣/٠٩/٢٦ وجد صاحب المشروع نسختين مثبّتتين («دلال» القديمة و«تالين») **برقم واحد 1.4.0** لأني غيّرت القارئ مرتين دون رفع الرقم — فلم يستطع التمييز، وكلتاهما تزحفان معًا فتضاعفان خطر الكابتشا، والقديمة تقرأ الوزن خطأً. صفحة الزاحف تحذّر الآن حين تتصل نسخة قديمة.
- **شريط تقدّم الإضافة (٢٣/٠٩/٢٦)**: الإضافة لا ترسل تقريرها إلا في آخر الدفعة (١٠٠ منتج ≈ ٣٠ دقيقة صمت)، فلم يعرف صاحب المشروع أهي تعمل. الخادم يستنتج التقدّم من مرورها عليه دون تغيير فيها: `GET /api/import/queue` = بداية دفعة، `POST /api/import` بـ`page_url='ext:stock'` أو `/api/import/check` = منتج، `/api/crawl/report` = نهاية. الحالة في صفّ `crawler_live` (ترحيل 0025)، وتُقرأ بـ`liveState` في `src/lib/crawl-live.ts`، وتُرسم في `/admin/crawler` وتتجدّد كل ٥ ثوانٍ من `/admin/crawler/live`، ونافذة الإضافة تقرؤها من `/api/crawl/live`. ثلاث دقائق بلا منتج = «توقفت في منتصف الدفعة».
- **`enriched` كان كذبًا**: `importProducts` عدّه مع كل مرور فكتب «مُثرى ١٠٠ من ١٠٠» دائمًا. صار يُعدّ فقط إن أُضيف فعلًا شيء (صور، مقاسات، وزن، عنوان)، وتعيد الدالة `gain {img,vars,wt}`، ويحفظ التقرير الأعمدة `gain_*` في `crawl_runs`.
- **الدفعات كانت تموت صامتة (مساء ٢٣/٠٩/٢٦)**: ثلاث دفعات متتالية عند صاحب المشروع قرأت ١٧ ثم ٠ ثم ٤ منتجات وسكتت، وكل ربع ساعة تبدأ دفعة جديدة. السبب: صفحة 1688 لا يكتمل تحميلها ⟵ `executeScript` ينتظر المستند **بلا حدّ** ⟵ بعد ٥ دقائق يقتل كروم عامل الخلفية ⟵ لا تقرير. أُعيد إنتاجه بصفحة وهمية لا تنتهي (`/tmp/fx-hang` في `server.mjs`، `HANG=1` في `live-bar.mjs`). العلاج في 1.5.3: مهلة ٣٠ ثانية للتحميل و٦٠ للمنتج كله (`within`)، `injectImmediately`، ونداء `getPlatformInfo` كل ٢٠ ثانية طوال الدفعة. **أي انتظار في الإضافة بلا مهلة = دفعة تموت يومًا ما.**
- **الصفحة التي لم تُحمَّل ليست «غير متوفر»**: القراءة الناقصة كانت تعطي «بلا سعر» فيُرسَل `/api/import/check` بـ`inStock:false` ويُنزَل منتج متوفر من الرف. الآن تُرسَل `skipped:true`: تأخير في الطابور (`enrich_tries+1`) و«لم يُقرأ» في الشريط، ولا تُكتب `last_checked_at`.
- دفعة بلا تقرير تُسجَّل عند بداية التالية في `crawl_runs` بحالة `partial` وملاحظة «انقطعت الدفعة بعد N»، فلا يضيع ما أضافته من إحصاء ٢٤ ساعة.
- اختبارات الإضافة الحقيقية بملف تعريف **جديد** كل مرة: الملف المحفوظ أبقى عامل الخلفية القديم يعمل بعد تغيير الشيفرة فبدا الإصلاح فاشلًا وهو سليم.
- الفحص على الإضافة الحقيقية: `xvfb-run -a node scripts/ext-test/live-bar.mjs` (مع `server.mjs` على 443 والموقع على 8787) — يشغّل دفعة من ٤ بإيقاعها البشري ويؤكد أن الصفحة المفتوحة رأت ١ ثم ٢ ثم ٣ ثم ٤ بلا إعادة تحميل، ويعيد مهمة الإثراء كما كانت.
- اختبار القارئ: `xvfb-run -a node scripts/ext-test/parse-test.mjs` على `scripts/ext-test/detail-nologin.html` المطابق للبنية الحقيقية.

## مزوّد 1688 API (OTAPI) — ما ثبت عمليًا على الموقع الحي
- الإعدادات في جدول `settings`: `src_provider=otapi`, `src_base_url=https://otapi.net`, `src_key` (instanceKey — لا يُكتب أبدًا في المستودع), `src_lang=en` (يعيد العنوان الأصلي الصيني + ترجمة إنجليزية تُستخدم كمساعد للترجمة العربية).
- الطرق التي تعمل فعلًا: `GetItemFullInfo?itemId=abb-{id}` (الجذر `OtapiItemFullInfo`)، و`SearchItemsFrame` (النتائج في `Result.Items.Content`). `BatchSearchItemsFrame` يحتاج `blockList=SearchItems`.
- منتج غير موجود يعيد `ErrorCode=NotFound` (المنتج نزل من 1688) فيُعلَّم غير متوفر.
- الفحص الحي بلا تسجيل دخول: workflow `source-check.yml` (يستخدم `IMPORT_TOKEN`) → `/api/source/test|run|translate`. الردود الخام تُسجَّل في `payment_log` بعنوان يبدأ بـ `SRC ` مع إخفاء المفتاح.
- قراءة قاعدة الإنتاج مباشرة (أسرع قناة تشخيص، تُغني عن تشغيل workflow لمجرد سؤال): قاعدة D1 الحية اسمها `dlal-db` ومعرّفها `b45c2ed9-976b-4c4f-a9ae-162b8664fdaf`، تُستعلم عبر موصّل Cloudflare من بيئة المساعد. **القراءة حرّة، والكتابة ممنوعة** على المنتجات والطلبات والزبائن وكل بيانات العمل: هذه تمر بالكود المنشور وباختبار e2e لا باستعلام مباشر. الاستثناء الوحيد: **مفتاح إعداد في جدول `settings`** حين يطلبه صاحب المشروع ولا يستطيع ضبطه من اللوحة — تُكتب بنفس جملة معالج النموذج (`INSERT … ON CONFLICT(key) DO UPDATE`)، ثم تُقرأ للتأكيد، ويُبلَّغ صاحب المشروع بالقيمة ووقتها. (طُبِّق في ٢٢/٠٩/٢٦ لضبط `src_month_limit=10000`.)
- الترجمة (مقيسة على الموقع الحي بـ `/api/source/models` في ٢٢/٠٩/٢٦، لا تُخمَّن): تعمل `llama-4-scout-17b` (أدقها، 645ms)، `llama-3.3-70b-fp8-fast` (876ms)، `mistral-small-3.1-24b` (أسرعها، 263ms)، `llama-3.1-8b-instruct-fp8` (738ms). **ملغاة أو ممنوعة:** `llama-3.1-8b-instruct` و`qwen1.5-14b` (5028)، `qwen2.5-14b` (غير موجود)، `gemma-3-12b` (5018 ممنوع للحساب). و`m2m100` يُنتج تكرارًا فارغًا («سباحة سباحة…») فحُذف من السلسلة.
- النموذج يترك أحيانًا كلمة صينية داخل عربية سليمة («عباءة طويلة بتصميم豹») — `dropCJKWords` تحذف تلك الكلمة بدل رفض العنوان كله؛ ما بقي صينيًا في أغلبه يُرفض.
- منتج لم تكتمل ترجمته يُحفظ `status='draft'` فلا يصل للمتجر. `releaseHeldDrafts` تنشره لحظة صيرورة عنوانه عربيًا **أيًّا كان الطريق** (ترجمة، تحرير من الأدمن، استيراد ثانٍ) لا فقط حين تُغيّره دفعة الترجمة نفسها — بلا استدعاء نموذج، ومقصورة على ما حجزه النظام (`title_src` موجود). الإخفاء المتعمّد حالته `hidden` لا `draft`. `chineseVisible` و`heldDraft` في الإحصاءات يجب أن يبقيا صفرًا.
- قيم المقاسات الرقمية («8号» = مقاس ٨ في الخواتم) يترجمها القاموس في `dictTranslate` بلا نموذج؛ النموذج كان يعيدها كما هي. أي قيمة متغيّر تتكرر وتعلق ⟵ أضفها إلى `DICT` بدل انتظار الذكاء الاصطناعي.
- التجربة المجانية 300 استدعاء/5 أيام: قلّل `max_pages`/`max_new` في `crawl_jobs` قبل أي تشغيل تجريبي.

## كريدت TMAPI — مقيس من لوحة الحساب وسجل الإنتاج في ٢٢/٠٩/٢٦، لا يُخمَّن
- **سعر الاستدعاء = ٢٠ كريدت** (`basic unit price` في تلميح اللوحة؛ قد يختلف باختلاف الـendpoint). الباقة ٢٠٠٠٠٠ كريدت/شهر = **١٠٠٠٠ استدعاء**.
- الفترة الأولى تُجزَّأ: اللوحة أظهرت حصة ٦٦٦٦٧ كريدت (= ٢٠٠٠٠٠ ÷ ٣) لأن الاشتراك بدأ وسط الدورة. التصفير الكامل عند `PERIOD END`. **قبل شراء أي رصيد راجع تاريخ التصفير: قد تصل الحصة مجانًا خلال أيام.**
- التحقق من أن الاستهلاك كله من موقعنا: `66660 ÷ 20 = 3333` استدعاء مقابل ٣٤٢٣ في `payment_log` (فارق ٢٫٦٪ = استدعاءات رُفضت لتجاوز السرعة ولم تُحاسَب). أي فارق كبير هنا يعني أن المفتاح مستعمل خارج الموقع.
- **PAYG خطر**: خيار «التحويل التلقائي إلى الدفع حسب الاستهلاك بعد نفاد الحصة» يسحب من المحفظة بصمت. يبقى **مطفأً** وتبقى الحصة سقفًا. مع `src_month_limit` كسقف ثانٍ من عندنا.
- حساب التكلفة قبل أي تشغيل كبير: **عدد المنتجات × ٢٠ كريدت**. البحث يعطي أفضل مردود (١٢٤٥ استدعاءً = ٢٤٩٠٠ كريدت أنتجت ١٤٩١٩ منتجًا)؛ التفصيل هو المكلف، فاستدعاء واحد لكل منتج لا أكثر.

## درس التسريب (٢٢/٠٩/٢٦): ٥١٪ من حصة فترة كاملة ضاعت على تكرار — لا تعِد السؤال عن منتج
- ثلاثة عيوب مجتمعة أنفقت ٢٣١٧ استدعاء تفصيل على **٦١٨ منتجًا فقط** (أحدها ١٩٠ مرة) بينما ١٣٤٥٠ منتجًا لم يُسأل عنه قط:
  1. استعلام الإثراء بلا ذاكرة: ترتيبه `(sales*10+views) DESC, id` وكل منتج مستورد `sales=0, views=0` فانهار إلى `id` وأعادت كل دفعة اختيار نفس الصفوف. **أي استعلام يختار عملًا مدفوعًا يجب أن يرشّح ويرتّب بـ`last_checked_at`.**
  2. نص خطأ المزوّد لا يتطابق: TMAPI ترد `"Item not found"` بمسافة والحارس كان `/NotFound/i`. **اختبر نص الخطأ الحقيقي من `payment_log` لا المتوقَّع.**
  3. الاستيراد يدّعي فحصًا لم يحدث (`last_checked_at=datetime('now')` عند الإدراج) فيُخفي المنتج عن دورة الإثراء. **لا يُكتب الحقل إلا عند فحص فعلي؛ يبقى NULL وإلا كذب.**
- منتج يردّ المزوّد أنه غير موجود ⟵ `status='unavailable'` فورًا فيخرج من الدورة نهائيًا.
- المنتج الذي لا يعطي المزوّد وزنه يبقى «ناقصًا» أبدًا: مهلة `src_enrich_cooldown_h` (٧٢ ساعة على الأقل) تمنع إعادة شرائه كل ساعة.
- الفحص الحارس في `scripts/e2e.mjs`: خادم وهمي **يعدّ** الاستدعاءات ويؤكد أن الدفعة الثانية تسأل عن منتجات أخرى تمامًا (التكرار = صفر). أي تعديل على حلقة الإثراء يجب أن يُبقيه أخضر.
